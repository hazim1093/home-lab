name: Secret Scan

on:
  pull_request:
  push:
    branches:
      - main

# The called workflow comments the offending files on the PR.
permissions:
  contents: read
  pull-requests: write

jobs:
  secret-scan:
    uses: hazim1093/ci/.github/workflows/secret-scan.yml@main
