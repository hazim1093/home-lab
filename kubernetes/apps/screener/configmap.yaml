apiVersion: v1
kind: ConfigMap
metadata:
  name: screener-config
  namespace: screener
data:
  LLM_PROVIDER: "openrouter"
  LLM_MODEL: "inclusionai/ling-3.0-flash:free" # Available openrouter/free — lazy option, routes to whatever free model is available (fine for low-stakes tasks)
