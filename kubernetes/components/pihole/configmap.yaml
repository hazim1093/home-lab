apiVersion: v1
kind: ConfigMap
metadata:
  name: pihole-custom-dnsmasq
  namespace: pihole
data:
  # Custom DNS records - update TRAEFIK_IP with your Traefik LoadBalancer IP
  02-custom.conf: |
    address=/homeassistant.local/TRAEFIK_IP
    address=/pihole.local/TRAEFIK_IP
