apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: grafana-resources
  namespace: flux-system
spec:
  dependsOn:
  - name: grafana-operator
  interval: 10m0s
  path: ./kubernetes/components/grafana-resources
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age
